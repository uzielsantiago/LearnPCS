<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uziel.splash">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/senas"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AprenderActivity"></activity>
        <activity android:name=".interNoticias" />
        <activity android:name=".InformateActivity" />
        <activity android:name=".ListaActivity" />
        <activity android:name=".visorPDFActivity" />
        <activity android:name=".Activity_visor_p_d_f" />
        <activity android:name=".LibrosActivity" />
        <activity android:name=".LoginGoogle" />
        <activity android:name=".RegistrarseActivity" />
        <activity android:name=".IniciarSesionActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>